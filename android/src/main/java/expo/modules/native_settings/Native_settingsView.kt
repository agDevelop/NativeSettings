package expo.modules.native_settings

import android.content.Context
import expo.modules.kotlin.AppContext
import expo.modules.kotlin.views.ExpoView

class Native_settingsView(context: Context, appContext: AppContext) : ExpoView(context, appContext)
